<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.ariessa.eventlagi">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".User.LogIn" />
        <activity
            android:name=".User.MainActivity"
            android:label="@string/title_activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Forms.NameDateTimeLocation" />
        <activity android:name=".Forms.VIPPersonInCharge" />
        <activity android:name=".Forms.FocusGroupCategories" />
        <activity android:name=".Forms.Fees" />
        <activity android:name=".Forms.Caterer" />
        <activity
            android:name=".Role"
            android:label="@string/title_activity_role"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>